1. Ensure you have node installed
2. Run `npm install` inside root
3. Run `npm run start` to run CLI app.
4. Run `npm run test` to run tests

Available (hardcoded) customers are found in `src/infra/customer-balance.repository.ts`.
